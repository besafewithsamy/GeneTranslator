import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.besafewithsamy.genetranslator',
  appName: 'GeneTranslator',
  webDir: 'dist'
};

export default config;
